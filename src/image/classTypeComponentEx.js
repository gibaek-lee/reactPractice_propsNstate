class ImClassTypeComponent extends Component {
  static defaultProps = {
    //initialize props if parent component doesn't send props
    //contact with 'this.props'
  }

  state = {
    //local variables
    //contact with 'this.state'
  }

  handleJSXevents = (e) => {
    //definition
  }

  LifeCycleAPI(prevProps, nextProps){
    //definition
  }

  render() {
    //some javascript code you need

    return (
      //JSX
      //show html, set a event handler, send props
      //ex: <button onClick = handleJSXevents></button>
    )
  }
}
